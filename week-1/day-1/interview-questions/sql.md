# create table
# load csv into postgressql
# view the table
# compute total_sales
# filter >50
# preview first 10 rows
# add total_sales as a real col in postgress
# update col values  for total_sales
# noow run queries directly on totl_sales


## Did you compute Total_Sales in SQL or store it?"

Answer:

"I first computed it dynamically using SQL expressions, and then optionally stored it as a column for reuse and performance optimization."
